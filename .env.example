# Copia este archivo como .env para desarrollo local

# --- OPCIÓN A (recomendada): worker local ---
# Ejecuta en otra terminal: npm run dev:worker
VITE_API_URL=http://localhost:8787

# --- OPCIÓN B: worker remoto de producción ---
# VITE_API_URL=https://api.intimaexclusive.com

# --- Opcional: activa SOLO si compraste Cloudflare Image Transformations ---
# VITE_ENABLE_IMAGE_TRANSFORMS=true

# --- Opcional: Meta (Facebook) Pixel ---
# El sitio ya trae el pixel de producción por defecto (hardcodeado en
# src/lib/metaPixel.js). Define esta variable solo si quieres usar OTRO pixel
# (ej: uno de pruebas) sin tocar el código.
# VITE_META_PIXEL_ID=2309058572967283
